{
  "name": "growl-audio-landing",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "upload": "aws s3 cp artifacts/gFractor-macOS.pkg s3://$BUCKET_NAME/downloads/gFractor-macOS.pkg && aws s3 cp artifacts/gFractor-Windows.exe s3://$BUCKET_NAME/downloads/gFractor-Windows.exe && aws s3 cp artifacts/gFractor-Linux.deb s3://$BUCKET_NAME/downloads/gFractor-Linux.deb"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
