{
  "name": "growl-audio-landing",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "upload": "aws s3 cp artifacts/gFractor-mac.zip s3://$BUCKET_NAME/downloads/gFractor-mac.zip && aws s3 cp artifacts/gFractor-win.zip s3://$BUCKET_NAME/downloads/gFractor-win.zip && aws s3 cp artifacts/gFractor-linux.zip s3://$BUCKET_NAME/downloads/gFractor-linux.zip"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
